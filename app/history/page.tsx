"use client";
import {useEffect,useMemo,useState} from "react";
import {useRouter} from "next/navigation";
type Item={id:string;tool_type:string;language:string;input_text:string;output_text:string;created_at:string};
export default function History(){const [items,setItems]=useState<Item[]>([]);const [q,setQ]=useState("");const [loading,setLoading]=useState(true);const router=useRouter();
 useEffect(()=>{fetch("/api/history").then(r=>r.json()).then(d=>setItems(d.items||[])).finally(()=>setLoading(false))},[]);
 const filtered=useMemo(()=>items.filter(x=>(x.tool_type+" "+x.language+" "+x.input_text+" "+x.output_text).toLowerCase().includes(q.toLowerCase())),[items,q]);
 async function del(id:string){if(!confirm("Delete this generation?"))return;await fetch("/api/delete",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,table:"generations"})});setItems(x=>x.filter(i=>i.id!==id))}
 async function save(i:Item){await fetch("/api/save",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({title:i.input_text.slice(0,60),type:i.tool_type,content:i.output_text})});alert("Saved to My Content.")}
 return <main style={{maxWidth:1000,margin:"0 auto",padding:20}}><header style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:24}}><div><div style={{fontWeight:900,fontSize:22}}>Creator<span style={{color:"#8b7cff"}}>Flow</span> AI</div><h1>History</h1></div><button className="cf-btn" onClick={()=>router.push("/dashboard")}>← Dashboard</button></header><input className="cf-input" placeholder="Search your generations..." value={q} onChange={e=>setQ(e.target.value)}/>{loading?<p className="cf-muted">Loading...</p>:filtered.length===0?<div className="cf-card" style={{padding:24,marginTop:16}}>No generations found.</div>:filtered.map(i=><article className="cf-card" style={{padding:20,marginTop:16}} key={i.id}><div style={{display:"flex",justifyContent:"space-between",gap:12}}><div><b>{i.tool_type}</b><span className="cf-muted"> · {i.language} · {new Date(i.created_at).toLocaleString()}</span><p className="cf-muted">{i.input_text}</p></div><div style={{display:"flex",gap:8}}><button className="cf-btn" onClick={()=>navigator.clipboard?.writeText(i.output_text)}>Copy</button><button className="cf-btn" onClick={()=>save(i)}>Save</button><button className="cf-btn" onClick={()=>del(i.id)}>Delete</button></div></div><pre style={{whiteSpace:"pre-wrap",fontFamily:"inherit",lineHeight:1.6}}>{i.output_text}</pre></article>)}</main>}
