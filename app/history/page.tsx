"use client";
import {useEffect,useMemo,useState} from "react";
import {useRouter} from "next/navigation";
type Item={id:string;tool_type:string;language:string;input_text:string;output_text:string;created_at:string};
export default function History(){const [items,setItems]=useState<Item[]>([]);const [q,setQ]=useState("");const [loading,setLoading]=useState(true);const [action,setAction]=useState("");const router=useRouter();
 useEffect(()=>{fetch("/api/history").then(r=>r.json()).then(d=>setItems(d.items||[])).catch(()=>setItems([])).finally(()=>setLoading(false))},[]);
 const filtered=useMemo(()=>items.filter(x=>(x.tool_type+" "+x.language+" "+x.input_text+" "+x.output_text).toLowerCase().includes(q.toLowerCase())),[items,q]);
 async function del(id:string){if(!confirm("Delete this generation?"))return;const r=await fetch("/api/delete",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,table:"generations"})});if(r.ok)setItems(x=>x.filter(i=>i.id!==id));else alert("Could not delete this generation.")}
 async function copy(text:string){try{await navigator.clipboard?.writeText(text);setAction("Copied!");setTimeout(()=>setAction(""),1600)}catch{setAction("Copy failed");setTimeout(()=>setAction(""),1600)}}
 async function save(i:Item){setAction("Saving...");try{const r=await fetch("/api/save",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({title:i.input_text.slice(0,60),type:i.tool_type,content:i.output_text})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Save failed");setAction("Saved!");setTimeout(()=>setAction(""),1600)}catch(e){setAction(e instanceof Error?e.message:"Save failed");setTimeout(()=>setAction(""),2200)}}
 return <main style={{maxWidth:1000,margin:"0 auto",padding:20}}><header style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:24,gap:12,flexWrap:"wrap"}}><div><div style={{fontWeight:900,fontSize:22}}>Creator<span style={{color:"#8b7cff"}}>Flow</span> AI</div><h1>History</h1></div><div style={{display:"flex",gap:8,alignItems:"center",flexWrap:"wrap"}}>{action&&<span className="cf-muted" style={{fontSize:13}}>{action}</span>}<button className="cf-btn" onClick={()=>router.push("/dashboard")}>← Dashboard</button></div></header><input className="cf-input" placeholder="Search your generations..." value={q} onChange={e=>setQ(e.target.value)}/>{loading?<p className="cf-muted">Loading...</p>:filtered.length===0?<div className="cf-card" style={{padding:24,marginTop:16}}>No generations found.</div>:filtered.map(i=><article className="cf-card" style={{padding:20,marginTop:16}} key={i.id}><div style={{display:"flex",justifyContent:"space-between",gap:12,alignItems:"flex-start",flexWrap:"wrap"}}><div style={{minWidth:0,flex:1}}><b>{i.tool_type}</b><span className="cf-muted"> · {i.language} · {new Date(i.created_at).toLocaleString()}</span><p className="cf-muted">{i.input_text}</p></div><div style={{display:"flex",gap:8,flexWrap:"wrap"}}><button className="cf-btn" onClick={()=>copy(i.output_text)}>Copy</button><button className="cf-btn" onClick={()=>save(i)}>Save</button><button className="cf-btn" onClick={()=>del(i.id)}>Delete</button></div></div><pre style={{whiteSpace:"pre-wrap",fontFamily:"inherit",lineHeight:1.6,overflowWrap:"anywhere"}}>{i.output_text}</pre></article>)}</main>}